# 써리힐즈 청소당번 자동화 시스템

이 프로젝트는 4인 거주(소현/은영/제이/승미) 청소 당번 로테이션을 계산하고,
카카오톡 공지문을 생성하고, 인증사진 제출 여부를 추적하는 도구다. 전체 설계
배경과 의사결정 근거는 `docs/design.md`를 참조한다.

## 핵심 원칙

- **운영자는 로테이션 대상이 아니다.** 소현/은영/제이/승미 4명만 당번을 돈다.
  운영자는 실행·확인·완료 입력을 전담하는 별도 역할이다.
- **완전 자동 발송이 아니다.** 카카오톡 API가 연동되어 있지 않으므로, 공지문
  붙여넣기와 "공지 완료" 확인은 항상 운영자의 수동 행위다.
- **사진은 시스템에 저장하지 않는다.** 평상시엔 당사자의 "완료" 신고를 그대로
  신뢰하고, 이의제기가 생겼을 때만 카카오톡 채팅방 원본을 사후 확인한다.
- **이름은 정확히 일치해야 처리한다.** "완료 처리" 명령에서 오타·별명은 자동
  추측하지 않고 재입력을 요청한다.

## 실행 트리거

매주 월요일 07:00(KST), 클라우드 루틴(claude.ai routines)이 자동으로 실행되어
아래 워크플로우를 돌리고 결과를 운영자에게 메시지로 먼저 보여준다. 이 저장소가
GitHub(`github.com/minjuuu101/sunnyhills-cleaning`)에 있고 클라우드 루틴이 이걸
체크아웃해서 실행하는 구조다. 운영자는 로컬(이 세션)에서도 언제든 같은 명령을
수동으로 실행할 수 있다 — 다만 로컬과 클라우드가 같은 저장소를 공유하므로
"데이터 동기화" 절을 반드시 지킨다.

## 데이터 동기화 (로컬 ↔ 클라우드)

`output/state.json`과 `checklist_history.json`은 로컬 세션과 클라우드 루틴이
번갈아 읽고 쓴다. 어느 쪽이든 상태를 바꾸는 스킬(위 세 스킬 전부)을 실행하기
**전에 반드시 `git pull`**, 실행하고 **바로 `git add output && git commit -m "..." && git push`**
한다. 이걸 건너뛰면 한쪽이 다른 쪽의 변경을 덮어써서 로테이션 인덱스나
디파짓 장부가 꼬일 수 있다.

## 매주 월요일 실행 순서

운영자가 "이번 주 당번표 만들어줘" 같은 요청을 하면, 아래 순서로 스킬을 호출한다.
(마감 처리를 로테이션 계산보다 먼저 해야 페널티 카운터가 최신 상태로 반영된다 —
설계서의 다이어그램 순서와 살짝 다르지만, 논리적으로 이 순서가 맞다.)

1. **checklist-tracker** `close-week` — 지난주 마감 처리 (확인대기 → 미제출 전환,
   연속 미제출 카운터 갱신, 2회차부터 디파짓 차감)
2. **rotation-generator** `run` — 이번 주 당번 매핑 계산 (결원 반영,
   같은 주 중복 실행 자동 차단)
3. **notice-formatter** `generate` — 공지문 생성. 지난주 "공지 완료" 확인이
   안 됐다면 여기서 먼저 안내 (최대 3회, 이후 자동 발송완료 간주)
4. 운영자에게 생성된 공지문 + 지난주 체크리스트 요약을 보여준다
5. 운영자가 카카오톡에 붙여넣고 "공지 완료"라고 말하면 → **notice-formatter**
   `confirm` 실행

## 주중 수시 처리

- 거주자가 카카오톡에 "청소완료" + 사진을 올리고 운영자가 이를 보면 →
  **checklist-tracker** `complete <이름>` 실행
- 거주자가 여행/병가로 결원을 미리 알리면 → **rotation-generator**
  `report-vacancy <이름>` 실행 (그 주 로테이션 계산 전에)
- 거주자가 이력을 보고 싶다고 하면 → **checklist-tracker** `summary`로 요약을
  뽑아서 공유 (상시 셀프 조회 권한을 주는 것이 아니라 요청 시 운영자가 공유하는
  방식)

## 로테이션 규칙

- 구역: 🚿 화장실 · 🍳 주방 · 🛋️ 공용공간 · ♻️ 분리수거 (세부 담당 내용은
  `output/state.json`의 `zone_details` 참조)
- 순환 방향: `output/state.json`의 `zones` 배열 순서가 곧 실제 순환 순서다
  (현재 주방→화장실→공용공간→분리수거→(다시 주방)). 이 순서는 임의로 바꾸면
  안 된다 — 실제 거주자들이 써오던 순서와 어긋나면 당번표가 틀어진다
  (2026-07-06에 한 번 이 순서가 design.md의 표시 순서와 뒤섞여 틀린 당번표가
  나간 적이 있다).
- 마감시각: 청소 당일 자정 + 10분 유예

## 디파짓 페널티 규칙

공용공간 구역 추가 방식의 페널티는 폐지되었다. 대신:

- 연속 미제출 1회차는 그냥 넘어간다 (차감 없음).
- 연속 미제출 2회차부터는 그 스트릭이 끊길 때까지 매회 개인 디파짓에서 $10씩
  차감하고, 동시에 4명 공동 풀(pool)에도 $10 적립된다.
- 공동 풀이 $100에 도달하면 회식비로 사용하고, 초과분만 남긴 채 리셋된다.
- 실제 돈을 걷고 보관하는 것은 운영자가 수동으로 한다 — 이 시스템은 장부
  (`output/state.json`의 `deposit`)만 관리한다.
- `checklist-tracker deposit-status`로 현재 장부/풀 현황을 조회할 수 있다.

## 예외 상황 에스컬레이션

아래는 스크립트가 처리하지 않으므로, 에이전트가 자연어로 상황을 파악해 운영자와
함께 `output/state.json`을 직접 조정하도록 안내한다:

- 거주자 영구 교체 (이사/신규 룸메이트) — 발생 빈도가 낮아 자동화하지 않음
- 명단/구역 자체의 변경 요청
- 로테이션 계산 결과가 스키마 검증에 실패하는 경우 (4명×4구역 1:1 매핑이 깨짐)

## 데이터

- `output/state.json` — 로테이션 인덱스, ISO 주차, 명단, 페널티 카운터, 이번 주
  당번표, 공지 상태
- `output/checklist_history.json` — 주차별 제출 이력. 3개월 지난 건은
  `checklist-tracker trim`으로 상세 삭제 후 요약만 유지
- `output/weekly_notice_YYYYMMDD.md` — 카카오톡 복붙용 공지문
